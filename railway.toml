[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python railway_init.py && gunicorn app:app --workers 2 --bind 0.0.0.0:$PORT --timeout 120"
healthcheckPath = "/login"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
