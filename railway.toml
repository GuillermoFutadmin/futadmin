[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python railway_init.py && gunicorn app:app --workers 4 --worker-class gevent --bind 0.0.0.0:$PORT --timeout 120"
healthcheckPath = "/ping"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[volumes]]
name = "uploads"
mountPath = "/app/static/uploads"
